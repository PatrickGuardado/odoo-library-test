<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_library_loan_form" model="ir.ui.view">
        <field name="name">library.loan.form</field>
        <field name="model">library.loan</field>
        <field name="arch" type="xml">
            <form string="Préstamo">
                <header>
                    <button name="action_return_book" type="object" string="Devolver Libro" class="oe_highlight" invisible="state == 'returned'"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <group>
                        <field name="partner_id"/>
                        <field name="book_id" readonly="1"/> <field name="loan_date"/>
                        <field name="return_date" readonly="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_library_loan_list" model="ir.ui.view">
        <field name="name">library.loan.list</field>
        <field name="model">library.loan</field>
        <field name="arch" type="xml">
            <list string="Préstamos">
                <field name="partner_id"/>
                <field name="book_id"/>
                <field name="loan_date"/>
                <field name="state" widget="badge" decoration-success="state == 'returned'" decoration-info="state == 'active'" decoration-danger="state == 'overdue'"/>
            </list>
        </field>
    </record>

    <record id="action_library_loan" model="ir.actions.act_window">
        <field name="name">Préstamos</field>
        <field name="res_model">library.loan</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_library_loans" name="Préstamos" parent="menu_library_root" action="action_library_loan" sequence="20"/>
</odoo>